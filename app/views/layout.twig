{% extends "master.twig" %}

{% from "components/panel.twig" import panel %}

{% block body %}

<div class="container-fluid">
  <div class="row">
    <div class="col-md-3">
      {{ panel('Title', 'Body') }}
    </div>
    <div class="col-md-6">
      <table class="table table-striped table-condensed">
        <thead>
          <tr>
            <th>Name</th>
            <th>Machine</th>
            <th>Location</th>
            <th></th>
          </tr>
        </thead>
        <tbody>
        {% include "components/service_row.twig" %}
        {% for service in services %}
          {% include "components/service_row.twig" %}
          {% include "components/service_row.twig" %}
          {% include "components/service_row.twig" %}
        {% endfor %}
        </tbody>
      </table>
    </div>
    <div class="col-md-3">
      {{ panel('Title', 'Body') }}
    </div>
  </div>
</div>

{% endblock %}
