{% spaceless %}
<div class="row service-header">
<h2 id="{{ service.name }}">
  {{ service.name|trim|default('Name') }}
  <span class="pull-right">
    {% if service.status is defined %}
      {% include "components/status_button.twig" with {'size' : 'sm'} %}
    {% endif %}
  </span>
</h2>

</div>
{% endspaceless %}

<div class="row">
{% block content %}
...
{% endblock content %}
</div>
